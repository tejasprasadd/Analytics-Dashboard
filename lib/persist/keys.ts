//This is a constant that is used to store the keys for the persisted state.
export const PERSIST_KEYS = {
  authToken: "dash:auth:token",
  authUser: "dash:auth:user",
  theme: "dash:theme",
  filters: "dash:filters",
} as const;
